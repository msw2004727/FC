# LINE Messaging API Channel Access Token
#
# 取得方式：
# 1. 前往 LINE Developers Console → https://developers.line.biz/
# 2. 選擇與 LIFF (2009084941) 相同的 Provider
#    （重要：必須在同一 Provider，userId 才會一致）
# 3. 建立或選擇 Messaging API channel
# 4. 在 "Messaging API" 分頁 → Issue channel access token (long-lived)
#
# 部署時使用 Firebase Secret Manager：
#   firebase functions:secrets:set LINE_CHANNEL_ACCESS_TOKEN
#
# 本檔僅供說明，實際 Token 請勿寫入此檔或提交至版本控制。
